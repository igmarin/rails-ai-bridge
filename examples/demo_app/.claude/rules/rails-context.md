# Rails semantic context

Auto-generated by rails-ai-bridge. Regenerate with `rails ai:bridge:claude`.

## Application

- **Name:** DemoApp
- **Rails:** 8.1.3.1
- **Ruby:** 3.3.12
- **Environment:** development

## Model classification

Tags guide AI attention:
- **core_entity** — configured primary domain models (`config.core_models`)
- **pure_join** — `has_many :through` join rows (FKs + timestamps only)
- **rich_join** — through join model with extra payload columns
- **supporting** — typical models, STI, or non-join tables

### core entity (2)
- Post
- User

### supporting (1)
- Comment

## Endpoint focus

- posts: 9 routes — `rails_get_routes(controller:"posts", detail:"summary")`
- rails/info: 4 routes — `rails_get_routes(controller:"rails/info", detail:"summary")`
- comments: 2 routes — `rails_get_routes(controller:"comments", detail:"summary")`
- rails/welcome: 1 routes — `rails_get_routes(controller:"rails/welcome", detail:"summary")`

## Pointers

- Schema tables: see `rails-schema.md` in this folder or `rails_get_schema` (MCP).
- Model details: `rails_get_model_details` with `model:"Name"`.
- Routes: start with `rails_get_routes(detail:"summary")`, then filter by controller.