# ActiveRecord Models (3)

For full details, use `rails_get_model_details` MCP tool.
Call with no args to list all, then `model:"Name"` for specifics.

- Comment (table: comments) — tier: supporting — 1 assocs, 2 validations
- Post (table: posts) — tier: core_entity — 2 assocs, 3 validations
- User (table: users) — tier: core_entity — 1 assocs, 2 validations